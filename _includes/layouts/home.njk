---
layout: layouts/base.njk
---
<div class="post-list">
  {%- for post in pagination.items %}
  <article class="post-card">
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
    <p>{{ post.content | excerpt }}</p>
    <a href="{{ post.url }}" class="read-more">Continue Reading &rarr;</a>
  </article>
  {%- endfor %}
</div>

{% if pagination.pages.length > 1 %}
<nav class="pagination" aria-label="Pagination">
  {% if pagination.href.previous %}
    <a href="{{ pagination.href.previous }}">&larr; Newer</a>
  {% else %}
    <span></span>
  {% endif %}
  <span>Page {{ pagination.pageNumber + 1 }} of {{ pagination.pages.length }}</span>
  {% if pagination.href.next %}
    <a href="{{ pagination.href.next }}">Older &rarr;</a>
  {% else %}
    <span></span>
  {% endif %}
</nav>
{% endif %}
